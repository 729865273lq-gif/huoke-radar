# 获客雷达 · 使用指南

## 一、启动

**以后每次使用**：双击项目文件夹里的 `run.bat`，浏览器会自动打开 `http://127.0.0.1:8080`。
（首次运行会自动装环境，只需一次。）

## 二、第一次使用要做的事（按顺序）

1. **填 AI Key**：`.env` 文件里的 `DEEPSEEK_API_KEY`（已配好就跳过）。
2. **登录发私信账号**：后台「② 发私信用的账号」→ 点「登录抖音」（或小红书）→ 弹出的浏览器里用手机 App 扫码。
3. **加同行**：把要盯的博主主页链接粘进「① 盯哪些同行」。
   - 抖音链接形如 `https://www.douyin.com/user/MS4wLjABAAAA...`
   - 小红书链接形如 `https://www.xiaohongshu.com/user/profile/5ff...`
4. **抓线索**：点「立即抓一轮」，或等后台每 10 分钟自动抓。

## 三、日常使用

1. 看「③ 线索流」：AI 已标出意图、拟好私信。
2. 点某条线索的「发送」→ 弹出一个浏览器窗口自动给对方主页发私信 → 发完自动标记「已发」。
3. 对方回复成交后，点「标记回复」记录。

## 四、防封号规则（重要）

- 默认每天最多发 **30 条**（`.env` 里 `DAILY_SEND_LIMIT` 可调）。
- 只对**主动评论**的人发**一条**，不轰炸。
- 建议每天实际发送量控制在十几条，模拟真人节奏。
- 私信内容不要自己改成"加微信"等话术，AI 生成的内容已规避。

## 五、常见问题

| 问题 | 处理 |
|---|---|
| 点发送提示"未登录" | 回「②」重新扫码登录一次 |
| 提示"找不到私信按钮" | 对方关闭了私信，换下一条；或告诉我 |
| 抓不到新评论 | 确认同行链接是「主页」链接；平台改版时告诉我来修 |
| 想换 AI 语气/业务描述 | 后台暂无设置项，直接找我改 |

## 六、多开实例（一个行业/客户开一个后台）

适合"给多个行业/客户运营"的场景：**双击 `多开.bat`**，按提示输入：

- **端口**：每个实例一个端口（8081、8082、8083…）
- **实例名**：比如行业拼音（jiancai / canyin / jiaoyu）

每个实例完全独立：自己的数据库、自己的浏览器身份、自己的抖音登录。所以：

| 实例 | 端口 | 用法 |
|---|---|---|
| 主实例（run.bat） | 8080 | 你自己用 |
| 多开实例 1 | 8081 | 建材客户（登录建材客户的抖音号） |
| 多开实例 2 | 8082 | 餐饮客户（登录餐饮客户的抖音号） |

**注意**：
- 实例越多，同一台电脑/同一网络的抓取越频繁，建议先开 2~3 个跑稳；
- 每个实例登录**不同**的抖音号（谁发私信就登谁的号）；
- 每个实例的数据存在 `data-实例名\` 文件夹里，删除该文件夹等于清空这个实例。

## 七、数据

- 所有数据存在本地 `data/leads.db`，不上传任何服务器。
- 不要删 `data/browser-profile` 文件夹，那是你的登录态。
